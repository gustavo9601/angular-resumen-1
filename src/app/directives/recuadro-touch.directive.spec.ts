import { RecuadroTouchDirective } from './recuadro-touch.directive';

describe('RecuadroTouchDirective', () => {
  it('should create an instance', () => {
    const directive = new RecuadroTouchDirective();
    expect(directive).toBeTruthy();
  });
});
